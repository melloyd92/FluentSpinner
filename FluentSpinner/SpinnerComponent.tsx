import * as React from 'react';
import { ISpinnerProps, Spinner } from '@fluentui/react';

function SpinnerComponent(props: ISpinnerProps) {
  return <Spinner {...props} />;
}

export default SpinnerComponent;
